{
  "name": "glxdefi",
  "version": "1.0.0",
  "description": "",
  "main": "package.json",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "compile": "npx oz compile --solc-version=0.6.2 --optimizer on",
    "test": "truffle test",
    "ropsten": "npx truffle deploy --network ropsten --reset",
    "goerli": "npx truffle deploy --network goerli --reset",
    "local": "npx truffle deploy --network local --reset"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/contracts": "^3.1.0",
    "@openzeppelin/contracts-ethereum-package": "^3.0.0",
    "truffle": "^5.1.37"
  },
  "devDependencies": {
    "@openzeppelin/cli": "^2.8.2",
    "@openzeppelin/test-helpers": "^0.5.6",
    "@truffle/hdwallet-provider": "^1.0.39",
    "chai": "^4.2.0",
    "dotenv": "^8.2.0"
  }
}
